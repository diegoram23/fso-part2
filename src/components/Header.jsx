
const Header = ({ courses }) => {

  return (
    <div>
      {/* {courses.map(title => {
        return <div key={title.id}>
          <h2>{title.name}</h2>
        </div>
      })} */}
    </div>
  );
}

export default Header;